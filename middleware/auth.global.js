
export default defineNuxtRouteMiddleware(async (to, from) => {
    // const accessToken = useState('accessToken');
    //
    // if (/manager-menu/ig.test(to?.fullPath) && !accessToken?.value) {
    //     return abortNavigation('Insufficient permissions.')
    // }
})